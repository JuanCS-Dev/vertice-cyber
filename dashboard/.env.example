# MCP Bridge Configuration
VITE_MCP_API_URL=http://localhost:8001
VITE_MCP_WS_URL=ws://localhost:8001/mcp/events

# Enable mock fallback (set to true to use local simulation if bridge unavailable)
VITE_ENABLE_MOCK_FALLBACK=false
